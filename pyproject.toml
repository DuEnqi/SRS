[project]
name = "srs-graphmem"
version = "1.0.0"
description = "SRS Fusion — GraphMem-ATMS backend + React frontend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "pydantic>=2.0.0",
]

[tool.vercel]
entrypoint = "api.index:app"
